import Reveal from "@/components/Reveal";

export default function ClosingBand() {
  return (
    <section className="journal-footer-band">
      <Reveal>
        <p>This is not our marketing.</p>
        <h2>It&apos;s the actual work, <em>as it happens.</em></h2>
      </Reveal>
    </section>
  );
}
